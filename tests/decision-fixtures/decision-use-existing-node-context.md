Route: merchant_standard_integration

Answer:

Since the provided package.json shows Node.js with Express, implement the checkout session flow in your Node.js / Express backend instead of asking for a different backend language.

