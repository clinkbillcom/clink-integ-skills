This response is routed to merchant agent integration with payment handoff.

The merchant server should implement POST /order/payment-session and GET /order/payment-session/{sessionId} as part of the payment session preparation flow.

