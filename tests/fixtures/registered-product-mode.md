This response is routed to merchant standard integration for checkout and webhook design.

Use registered product mode when the product already exists in Clink through the Dashboard or `clink catalog import`. In this mode, pass `productId` and `priceId`, make sure both values match the Clink records, and prefer catalog import when the merchant site already has discoverable products, prices, or subscription plans.

