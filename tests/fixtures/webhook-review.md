This review focuses on integration risk.

Check signature handling, idempotency, retry behavior, out-of-order delivery, `clink webhook endpoint ensure --events commerce` configuration for complete charging integrations, `CLINK_WEBHOOK_SIGNING_KEY` sync, and restart or redeploy after secret changes. The review should call out any risk if those parts are missing and reject a core-only production design as incomplete.

